export const AttributeState = Object.freeze({
  ACTIVE: Object.freeze({
    id: "active",
    name: "Active",
  }),
  DISABLED: Object.freeze({
    id: "disabled",
    name: "Disabled",
  }),
  DELETED: Object.freeze({
    id: "deleted",
    name: "Deleted",
  }),
});
